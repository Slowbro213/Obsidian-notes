## Fundamentals

Every Pod can communicate with every other Pod without the need for NAT.  
Each Pod receives its own unique IP address within the cluster.

This networking model is achieved through a CNI (e.g., Calico, Flannel, etc.).